import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: '#888',
    height: 48,
    borderRadius: 8,
    paddingHorizontal: 16,
    color: '#333',
  },
  inputFocus: {
    borderColor: '#222',
  },
});
