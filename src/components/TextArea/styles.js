import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  textarea: {
    minHeight: 72,
    maxHeight: 144,
    borderWidth: 1,
    borderColor: '#888',
    borderRadius: 8,
    paddingHorizontal: 16,
    paddingTop: 16,
    paddingBottom: 16,
    color: '#333',
  },
  textareaFocus: {
    borderColor: '#222',
  },
});
